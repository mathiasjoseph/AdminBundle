{% import 'MikyAdminBundle:Crud/Macro:breadcrumb.html.twig' as breadcrumb %}

{% set breadcrumbs = [
        { label: 'miky_admin.administration'|trans, url: path('miky_admin_dashboard') },
        { label: (metadata.applicationName~'.'~metadata.pluralName)|trans, url: path(configuration.getRouteName('index'), configuration.vars.route.parameters|default({})) },
        { label: 'miky.ui.new'|trans }
    ]
%}

{{ breadcrumb.crumble(breadcrumbs) }}
