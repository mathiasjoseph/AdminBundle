{#{% import 'MikyAdminBundle:Macro:buttons.html.twig' as buttons %}#}

{{ form(form) }}
{{ grid.requestConfiguration.getRouteName('validate') }}
{#{{ buttons.validate(path(grid.requestConfiguration.getRouteName('validate'), {'id': data.id})) }}#}
